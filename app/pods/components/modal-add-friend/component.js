//COMP components.modal-add-friend
import Ember from 'ember';

export default Ember.Component.extend({
	tagName: "button",
	classNames: ['pseudo button'],
  click() {
    alert("Fix this later");
  }
});
