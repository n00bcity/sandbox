//CTL friends.new
import Ember from 'ember';

export default Ember.Controller.extend({
/*	actions: {
    save() {
		  this.get('model.friends').save();
		  return true; // bubble to route
  	}
  }*/
});